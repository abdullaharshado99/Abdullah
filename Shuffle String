def restoreString(self, s, indices):
        final = ''
        for j in range(len(s)):
            final += s[indices.index(j)]
        return final
restoreString(None, "codeleet", [4,5,6,7,0,2,1,3])

// output = "leetcode"
