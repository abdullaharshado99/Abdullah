def arrayPairSum(self, nums):
        count = 0
        nums = sorted(nums)
        for i in range(0, len(nums), 2):
            count += nums[i]
        return count
arrayPairSum(None, [1, 4, 3, 2])

// output = 4
