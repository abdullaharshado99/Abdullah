def sortedSquares(self, nums):
        
        for i in range(len(nums)):
            nums[i] = abs(nums[i] ** 2)
        nums.sort()
        return nums
sortedSquare(None, [-4,-1,0,3,10])

// output = [0,1,9,16,100]
