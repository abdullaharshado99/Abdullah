def canBeEqual(self, target, arr):
        target.sort()
        arr.sort()
        if arr == target:
            return True
        return False
canBeEqual(None, [1, 2, 3, 4], [2, 4, 1, 3])

// output = True
