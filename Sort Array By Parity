def sortArrayByParity(self, nums):
        node = []

        for i in nums:
            if i%2==0:
                node.insert(0, i)
            else: 
                node.append(i)

        return node
sortArrayByParity(None, [3, 1, 2, 4])

// output = [2,4,3,1]
