def countConsistentStrings(self, allowed, words):
        count = 0
        for word in words:
           if set(word) <= set(allowed):
               count += 1
        return count
countConsistentStrings(None, 'ab', ["ad","bd","aaab","baa","badab"])

// output = 2
