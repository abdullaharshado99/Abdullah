def createTargetArray(self, nums, index):
        res  = []
        for i in range(len(nums)):
          for j in index:
              res.insert(j, nums[i])
              i += 1
          return res
createTargetArray(None, [0,1,2,3,4], [0,1,2,2,1])

// Output= [0,4,1,3,2]
