def luckyNumbers(self, matrix):
        
        node = list({min(row) for row in matrix}&{max(col) for col in zip(*matrix)})
        return node
luckyNumbers(None, [[3,7,8],[9,11,13],[15,16,17]])

//output = [15]
