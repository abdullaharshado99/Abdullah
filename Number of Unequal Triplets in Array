def unequalTriplets(self, nums):
        count = 0

        for i in range(len(nums)):
            for j in range(i+1, len(nums)):
                for k in range(j+1, len(nums)):
                    if nums[i] != nums[j] and nums[i] != nums[k] and nums[j] != nums[k]:
                        count +=1
        return count
unequalTriplets(None, [4,4,2,4,3])

//output = 3
