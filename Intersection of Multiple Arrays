def intersection(self, nums):
        nod = set(nums[0])
        for i in range(1, len(nums)):
            nod &= set(nums[i])
        nod = list(nod)
        nod.sort()
        return nod
intersection(None, [[3,1,2,4,5],[1,2,3,4],[3,4,5,6]])

#output= [3, 4]
