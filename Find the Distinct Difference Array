def distinctDifferenceArray(self, nums):
        node = []
        for i in range(len(nums)):
            nos = len(set(nums[:i+1])) - len(set(nums[i+1:]))
            node.append(nos)
        return node
distinctDifferenceArray(None, [1,2,3,4,5])

// output = [-3,-1,1,3,5]
