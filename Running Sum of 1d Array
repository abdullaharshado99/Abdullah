def runningSum(self, nums):
        num = 0
        res = []
        for i in range(len(nums)):
            num += nums[i]
            nums[i] = num
            res.append(num)
        return res
runningSum(None, [1,2,3,4])

// output = [1, 3, 6, 10]
